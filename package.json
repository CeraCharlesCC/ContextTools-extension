{
  "name": "context-tools-extension",
  "version": "0.1.1",
  "description": "Copy GitHub context into markdown.",
  "type": "module",
  "scripts": {
    "dev": "npm run dev:chrome",
    "dev:chrome": "cross-env TARGET_BROWSER=chrome VITE_BUILD_TARGET=main vite build --watch --mode development",
    "dev:firefox": "cross-env TARGET_BROWSER=firefox VITE_BUILD_TARGET=main vite build --watch --mode development",
    "build": "npm run build:chrome && npm run build:firefox",
    "build:chrome": "cross-env TARGET_BROWSER=chrome VITE_BUILD_TARGET=main vite build && cross-env TARGET_BROWSER=chrome VITE_BUILD_TARGET=content vite build",
    "build:firefox": "cross-env TARGET_BROWSER=firefox VITE_BUILD_TARGET=main vite build && cross-env TARGET_BROWSER=firefox VITE_BUILD_TARGET=content vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync:versions": "node scripts/sync-versions.mjs",
    "sync:versions:check": "node scripts/sync-versions.mjs --check",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rimraf dist dist-chrome dist-firefox"
  },
  "devDependencies": {
    "@anthropic-ai/claude-code": "^2.0.0",
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.0.10",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
